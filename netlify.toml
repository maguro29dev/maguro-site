[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# 旧 PWA manifest パスからのリダイレクト
[[redirects]]
  from = "/manifest.json"
  to = "/manifest.webmanifest"
  status = 301